{{ define "blog/view-fa.tmpl" -}}
    {{ template "base/header" . }}

    <div class="container">
        <div class="row">
            <div class="col s12">
                <h3>
                    {{ .title }}
                </h3>

                {{ .content }}
            </div>
        </div>
    </div>

    {{ template "base/footer" . }}
{{ end }}
